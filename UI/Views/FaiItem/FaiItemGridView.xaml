<UserControl x:Class="UI.Views.FaiItem.FaiItemGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Views.FaiItem"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid GridLinesVisibility="Horizontal" AutoGenerateColumns="False" Name="PART_DataGrid" CanUserAddRows="False" CanUserDeleteRows="False">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Name}" Header="Name"/>
            <DataGridTextColumn Binding="{Binding Value}" Header="Value"/>
            <DataGridTextColumn Binding="{Binding Passed}" Header="Passed"/>
            <DataGridTextColumn Binding="{Binding MinBoundary}" Header="Min"/>
            <DataGridTextColumn Binding="{Binding MaxBoundary}" Header="Max"/>
            <DataGridTextColumn Binding="{Binding Weight}" Header="Weight"/>
            <DataGridTextColumn Binding="{Binding Bias}" Header="Bias"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
