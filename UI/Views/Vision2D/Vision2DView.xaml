<UserControl x:Class="UI.Views.Vision2D.Vision2DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:halconDotNet="clr-namespace:HalconDotNet;assembly=halcondotnet"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <halconDotNet:HSmartWindowControlWPF Grid.Column="0"/>

        <GroupBox Header="Find line configs" Grid.Column="1">
            <Grid>
                <!-- Find line config list view -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0" HorizontalAlignment="Right"
                          ItemsSource="{Binding FindLineParams}"
                          CanUserSortColumns="True"
                          CanUserAddRows="False" />
                <!-- Run button -->
                <StackPanel Grid.Row="1">
                    <Button Content="Switch Socket"></Button>
                    <Button Content="Run" />
                </StackPanel>
            </Grid>
        </GroupBox>
        
        <Expander Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" FlowDirection="RightToLeft">
            <Expander.Header>
                <TextBlock
                    Text="Results"
                    RenderTransformOrigin=".5,.5">
                    <TextBlock.LayoutTransform>
                        <RotateTransform
                            Angle="90" />
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </Expander.Header>
            
            <!-- FaiItems list view -->
            <DataGrid ItemsSource="{Binding FaiItems}"></DataGrid>
        </Expander>
        
        
        <!-- Fai items -->
        <DataGrid Grid.Column="1"></DataGrid>
    </Grid>
</UserControl>